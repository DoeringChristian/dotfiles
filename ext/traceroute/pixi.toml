# Buildable pixi package for traceroute (Dmitry Butskoy's). Linux-only; not on
# conda-forge. Built from source.
# NOTE: only referenced from [target.linux-64.dependencies] -- builds on Linux
# only, NOT verified on macOS.

[package]
name = "traceroute"
version = "2.1.6"

[package.build.backend]
name = "pixi-build-rattler-build"
version = "*"
channels = ["https://prefix.dev/conda-forge"]
