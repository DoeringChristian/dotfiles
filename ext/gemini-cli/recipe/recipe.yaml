# gemini-cli -- Google's Gemini CLI, vendored from npm.
# No `source:` block: npm fetches the package itself during build.sh.
context:
  version: "0.47.0"

package:
  name: gemini-cli
  version: ${{ version }}

build:
  number: 0
  # The npm package vendors prebuilt native bits (node-pty); don't let
  # rattler-build patchelf/relocate them (also avoids needing patchelf on Linux).
  dynamic_linking:
    binary_relocation: false

requirements:
  build:
    - nodejs        # provides `npm` at build time
  run:
    - nodejs        # the `gemini` launcher runs under node

tests:
  - script:
      - gemini --version

about:
  homepage: https://github.com/google-gemini/gemini-cli
  license: Apache-2.0
  summary: Google Gemini command-line interface
