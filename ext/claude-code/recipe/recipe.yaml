# claude-code -- Anthropic's Claude Code CLI, vendored from npm.
# No `source:` block: npm fetches the package itself during build.sh.
context:
  version: "2.1.187"

package:
  name: claude-code
  version: ${{ version }}

build:
  number: 0
  # The npm package vendors its own prebuilt, self-contained native binary; don't
  # let rattler-build patchelf/relocate it (also avoids needing patchelf on Linux).
  dynamic_linking:
    binary_relocation: false

requirements:
  build:
    - nodejs        # provides `npm` at build time
  run:
    - nodejs        # the `claude` launcher runs under node

tests:
  - script:
      - claude --version

about:
  homepage: https://github.com/anthropics/claude-code
  license: LicenseRef-Anthropic
  summary: Claude Code -- agentic coding tool in your terminal
