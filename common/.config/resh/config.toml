shell = "fish"

[env]
# brew prefixes first; the mise shims are appended as a fallback for remotes not
# yet migrated to brew (drop them once every host is on brew).
PATH = "$PATH:$HOME/.local/bin:/opt/homebrew/bin:/home/linuxbrew/.linuxbrew/bin:$HOME/.homebrew/bin:$HOME/.local/share/mise/shims"
