# Sunshine configuration (stow-managed from dotfiles/common/.config/sunshine).
# Note: saving settings in the Sunshine Web UI rewrites this file through the
# stow symlink, i.e. straight into the dotfiles repo.

# Origins allowed to talk to the Web UI (CSRF protection). Comma-separated,
# each entry with protocol and host; the localhost variants are allowed by
# default, so only the names this machine is actually reached under go here.
csrf_allowed_origins = https://fermat:47990,https://fermat.tail935d43.ts.net:47990,https://100.81.242.65:47990
