# Ensure dotfiles (mise) programs are findable on the remote
env PATH=$PATH:$HOME/.local/bin:$HOME/.local/share/mise/shims

# Use fish on remote if available, otherwise falls back to default shell
login_shell fish
