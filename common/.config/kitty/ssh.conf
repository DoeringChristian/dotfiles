# Ensure dotfiles (brew) programs are findable on the remote
env PATH=$PATH:$HOME/.local/bin:/opt/homebrew/bin:/home/linuxbrew/.linuxbrew/bin:$HOME/.homebrew/bin

# Use fish on remote if available, otherwise falls back to default shell
login_shell fish
