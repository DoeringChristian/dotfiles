# pixi environments (built locally, never committed)
.pixi/*
!.pixi/config.toml

# nix leftovers
result
nvim.log

# macOS Finder junk
**/.DS_Store
# pixi-build / rattler-build outputs (never commit built packages)
ext/**/*.conda
*.conda
.pixi/
