# mise: local data dir (only created for sandboxed experiments; real installs
# go to ~/.local/share/mise) and per-machine overrides.
.mise/
mise.local.toml

# nix leftovers
result
nvim.log

# macOS Finder junk
**/.DS_Store
